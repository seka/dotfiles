# Gemini

Gemini AI エージェントの個別ガイドです。共通ルールは必ず `~/.gemini/AGENTS.md` を参照してください。

## 🧠 Codex 固有のスタイル

- **Plan モードが既定**: 作業開始時は Plan ツールを使い、各ステップ完了ごとに更新する。

## 🔍 作業の進め方

- **GitHub 操作は `gh` コマンドを優先**: PR 作成や Issue 参照は CLI で行うことを想定している。
